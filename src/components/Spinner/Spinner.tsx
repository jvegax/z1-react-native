import {Text, View} from 'react-native';
import React from 'react';

const Spinner = () => {
  return (
    <View>
      <Text>LOADING...</Text>
    </View>
  );
};

export default Spinner;
